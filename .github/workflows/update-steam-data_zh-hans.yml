name: Update Steam card zh-hans

on:
  schedule:
    - cron: '23 22 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-steam-card-zh-hans
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Update Steam data zh-hans
        env:
          STEAM_API_KEY: ${{ secrets.STEAM_API_KEY }}
        run: node js/update-steam-data_zh-hans.mjs

      - name: Commit changed data
        run: |
          if git diff --quiet -- index_zh-hans.html; then
            echo "Steam data did not change."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index_zh-hans.html
          git commit -m "chore: update Steam card data zh-hans"
          git push
